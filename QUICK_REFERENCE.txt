╔════════════════════════════════════════════════════════════════════════════════╗
║                                                                                  ║
║          Robotics Course Book - Backend Setup Quick Reference Card              ║
║                      (Gemini API + Qdrant Cloud, No Docker)                     ║
║                                                                                  ║
╚════════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: GET API KEYS (Both Free)                                             │
└──────────────────────────────────────────────────────────────────────────────┘

  🔑 Gemini API Key                    🔑 Qdrant Cloud Cluster
  ────────────────────                 ──────────────────────
  1. Go to ai.google.dev               1. Go to cloud.qdrant.io
  2. Click "Get API key"               2. Sign up (free tier)
  3. Copy the key                      3. Create a cluster
                                       4. Copy URL and API key

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: CREATE .env FILE                                                     │
└──────────────────────────────────────────────────────────────────────────────┘

  Copy .env.example → .env and fill in:

    GEMINI_API_KEY=your_gemini_key_here
    GEMINI_MODEL=gemini-pro
    QDRANT_URL=https://your-cluster.qdrant.io:6333
    QDRANT_API_KEY=your_qdrant_api_key_here

  ✓ Save the file

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: RUN SETUP SCRIPT (Pick ONE)                                          │
└──────────────────────────────────────────────────────────────────────────────┘

  🪟 Windows Batch (Easiest):              🪟 Windows PowerShell:
  ────────────────────────                 ─────────────────────
  > setup_backend.bat                      > .\setup_backend.ps1

  🐧 Linux/macOS (Manual):
  ───────────────────────
  $ python -m venv .venv
  $ source .venv/bin/activate
  $ pip install -r backend/requirements.txt
  $ python backend/scripts/validate_env.py
  $ python backend/scripts/populate_qdrant.py
  $ uvicorn backend.src.main:app --reload --port 8000

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 4: TEST BACKEND                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

  Open new terminal and run:

  ✓ Health Check
    curl http://localhost:8000/health
    → Expected: {"status":"ok"}

  ✓ Chat
    curl -X POST http://localhost:8000/chat \
      -H "Content-Type: application/json" \
      -d '{"query": "What is Physical AI?", "user_level": "Beginner"}'
    → Expected: JSON with response + sources

  ✓ Translate
    curl -X POST http://localhost:8000/translate \
      -H "Content-Type: application/json" \
      -d '{"text": "Hello", "target_language": "Urdu"}'
    → Expected: JSON with translated text

┌──────────────────────────────────────────────────────────────────────────────┐
│ STEP 5: RUN FRONTEND (Optional)                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  Open another terminal:

  $ cd frontend
  $ npm run start

  Then open http://localhost:3000 and try the chatbot!

┌──────────────────────────────────────────────────────────────────────────────┐
│ ENVIRONMENT VARIABLES (.env)                                                 │
└──────────────────────────────────────────────────────────────────────────────┘

  REQUIRED:
  ─────────
  GEMINI_API_KEY        Your Gemini API key (https://ai.google.dev)
  QDRANT_URL            Your Qdrant cluster URL (https://cloud.qdrant.io)
  QDRANT_API_KEY        Your Qdrant API key

  OPTIONAL:
  ────────
  GEMINI_MODEL          Model name (default: "gemini-pro")
  QDRANT_HOST           Localhost fallback (default: "localhost")
  QDRANT_PORT           Localhost fallback port (default: "6333")

┌──────────────────────────────────────────────────────────────────────────────┐
│ WHAT GETS CREATED                                                            │
└──────────────────────────────────────────────────────────────────────────────┘

  Backend API
  ───────────
  POST /chat              → Q&A using Gemini + Qdrant
  POST /translate         → Translate to Urdu using Gemini
  GET  /health            → Health check

  Vector Database
  ───────────────
  Qdrant Cloud collection "textbook_chapters"
  - Populated with all markdown from frontend/docs/book
  - Each doc has embedding + metadata (id, title, text snippet)

  LLM
  ───
  Gemini Pro (via API)
  - Handles chat responses
  - Handles translation

┌──────────────────────────────────────────────────────────────────────────────┐
│ FILES CREATED/MODIFIED                                                       │
└──────────────────────────────────────────────────────────────────────────────┘

  ✨ New Setup Files:
     .env.example                      ← Copy this to .env
     .env                              ← YOU create with your keys
     setup_backend.bat                 ← Windows batch setup
     setup_backend.ps1                 ← Windows PowerShell setup
     README_BACKEND.md                 ← Setup guide
     BACKEND_GEMINI_QDRANT.md          ← Architecture & detailed guide
     SETUP_BACKEND.md                  ← Very detailed walkthrough
     SETUP_CHECKLIST.md                ← Checklist to track progress

  🔧 Modified Core Files:
     backend/src/services/vector_store.py   ← Qdrant Cloud support
     backend/src/main.py                    ← Load .env
     backend/requirements.txt               ← Added google-generativeai, python-dotenv

  ✓ Already Working (No changes):
     backend/src/services/rag_service.py     ← RAG pipeline
     backend/scripts/populate_qdrant.py      ← Load markdown into Qdrant
     backend/src/api/chat.py                 ← Chat endpoint
     backend/src/api/translate.py            ← Translation endpoint

┌──────────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

  ❌ "Connection refused"
     → Check QDRANT_URL and QDRANT_API_KEY in .env
     → Verify Qdrant cluster is running in https://cloud.qdrant.io

  ❌ "google.generativeai not found"
     → pip install google-generativeai

  ❌ "LLM not configured" (dummy response)
     → .env file missing or GEMINI_API_KEY not set
     → Restart backend after editing .env

  ❌ "Collection not found"
     → Run: python backend/scripts/populate_qdrant.py

  ❌ Slow on first run
     → Normal! (2-5 minutes to download embeddings model + compute vectors)

┌──────────────────────────────────────────────────────────────────────────────┐
│ QUICK LINKS                                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

  📖 Documentation:
     • README_BACKEND.md              ← Start here
     • BACKEND_GEMINI_QDRANT.md       ← Detailed architecture
     • SETUP_BACKEND.md               ← Step-by-step walkthrough
     • SETUP_CHECKLIST.md             ← Checklist format

  🔗 External:
     • Gemini API:    https://ai.google.dev
     • Qdrant Cloud:  https://cloud.qdrant.io
     • FastAPI Docs:  http://localhost:8000/docs (after backend starts)

┌──────────────────────────────────────────────────────────────────────────────┐
│ NEXT STEPS                                                                   │
└──────────────────────────────────────────────────────────────────────────────┘

  1. ✅ Get API keys (Gemini + Qdrant)
  2. ✅ Create .env file with credentials
  3. ✅ Run setup_backend.bat (or manual steps)
  4. ✅ Test with curl commands
  5. 🚀 Run frontend and use the chatbot!

╔════════════════════════════════════════════════════════════════════════════════╗
║                         You're All Set! Good Luck! 🚀                          ║
╚════════════════════════════════════════════════════════════════════════════════╝
